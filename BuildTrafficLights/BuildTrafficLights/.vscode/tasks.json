// A task runner configuration.
{
    "version": "0.1.0",
    "command": "grunt",
    "isShellCommand": true,
    "tasks": [
         {
            "taskName": "ts:build",
            "isBuildCommand": true,
            "problemMatcher": "$msCompile"
        },
      {
        "taskName": "exec:package",
        "isBuildCommand": true,
        "problemMatcher": "$msCompile"
      }
    ]
}